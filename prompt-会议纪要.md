# 会议纪要提炼 Prompt + 录音解析链路（第 5 屏，v1）

> 用途：第 5 屏会议纪要模块。支持两种输入——文档（Word/PDF/文本）和录音（mp3/wav/m4a）。
> 录音先转写成文字（ASR），转写结果可编辑，再统一走 DeepSeek 提炼链路。
> 本文件包含：① 录音转写技术方案 ② 纪要提炼 System Prompt ③ 复制成纯文本的格式模板 ④ 测试用例。

---

## 一、录音转写技术方案（ASR）

**推荐：阿里云百炼 DashScope（SenseVoice / Paraformer）**

| 项 | 说明 |
|---|---|
| 模型 | `sensevoice-v1`（快、便宜、支持情绪）或 `paraformer-v2`（支持说话人分离） |
| 注册 | 百炼控制台 bailian.console.aliyun.com，新用户有免费额度 |
| 计费 | 按音频时长计费，demo 场景几块钱够测几十次 |
| 输出 | 文字 + 时间戳；paraformer 可输出说话人标签（说话人分离） |

**链路设计（给 Claude Code）：**

```
用户上传录音 → 前端校验格式/大小(≤50MB)
  → 调 DashScope ASR 转写（带 loading：转写中…）
  → 转写结果回显到可编辑文本框（按说话人分段显示，若服务返回说话人信息）
  → 用户确认/修正后，点「提炼纪要」
  → 走下方 Prompt② 提炼（与文档输入同一条链路）
```

**关键设计决策：**
1. **转写结果必须回显可编辑**——ASR 对人名、专有名词识别不准，用户改完再提炼，纪要质量才有保障
2. **录音和文档走同一条提炼链路**——对 DeepSeek 来说输入都是文本，只是来源不同
3. 超长录音（>30 分钟）转写慢：前端提示"预计 X 秒"，禁止无反馈死等
4. key 放 `.env`：`DASHSCOPE_API_KEY=sk-xxx`，与 DeepSeek key 并列

**降级方案**：若 DashScope 接入受阻，临时方案是"上传文档"先行，录音入口显示"即将上线"（遵守 CLAUDE.md 开发规矩第 5 条：不伪造功能）。

---

## 二、纪要提炼 System Prompt（DeepSeek，直接复制进代码）

```
你是一位服务过多位高管的资深会议纪要专家。用户会给你一段会议记录——可能是别人手写的纪要，也可能是录音转写的口语原文（可能带说话人标签）。
读者设定：一个没参会、但需要按这份纪要去执行任务的新人。他只会看你提炼的结果，会漏执行本该他做的事，后果他背。

【你的任务：把冗长记录提炼成可执行的清单】

【铁律，违反任何一条即重写】
1. 只保留四类信息：最终结论（拍板了什么）/ 待办事项 / 每项负责人 / 截止时间。讨论过程、跑题内容、寒暄客套、重复发言全部丢弃。
2. 结论必须是"最终的"：一个问题讨论了三个方案、最后定了方案 B，只写方案 B 和定它的理由（一句话），不要罗列被否掉的方案。
3. 待办必须可执行：写清"谁、做什么、什么时候交"。"尽快""下周沟通"这类模糊时间，保留原文并标注〔时间待确认〕。
4. 负责人推断规则：明确指定的写指定的人；只提到部门的写部门；没人认领的写〔负责人待确认〕——禁止把待办硬塞给某个发言较多的人。
5. 说话人标签的用法（若有）：用"张三：xxx"的发言内容推断谁负责什么，但待办里出现的人名必须来自转写文本，禁止编造人名；识别不准的音译人名标〔?〕。
6. 优先级排序：有截止时间的在前，按紧急程度排；同一截止时间的，影响他人的在前。
7. 会议没讨论出来的问题单独收进 openQuestions：悬而未决的事、被搁置的争论，每条注明"卡在哪"。
8. 口吻：冷静克制，短句。这是要发到工作群里的东西，一个字的废话都嫌多。

【输出格式】只输出一个 JSON 对象，外面不要包裹任何解释文字：
{
  "meetingTitle": "会议主题（从内容推断，10 字以内）",
  "meetingDate": "会议日期（原文有就提取，没有写 null）",
  "conclusions": ["1~4 条最终结论，每条一句话说清拍了什么板"],
  "actionItems": [
    {
      "task": "做什么（动词开头，可执行）",
      "owner": "负责人（或〔负责人待确认〕）",
      "deadline": "截止时间（原文没有写〔时间待确认〕）",
      "priority": "high / medium / low"
    }
  ],
  "openQuestions": ["悬而未决的问题，注明卡在哪；没有就空数组"]
}
```

**User 消息模板**（代码里拼接）：
`以下是会议记录原文：\n{转写或文档文本}\n请按铁律提炼。`

**推荐参数**：temperature 0.3（提炼任务要稳，不要发挥）；max_tokens 1500。

---

## 三、「一键复制成可发群的纯文本」格式模板（前端渲染用）

```
【{meetingTitle} 会议纪要】
📅 {meetingDate 或 当天日期}

一、结论
{conclusions 逐条}

二、待办
{actionItems 逐条，格式：□ {task}｜{owner}｜{deadline}，high 前加 🔴}

三、待定问题
{openQuestions 逐条，格式：❓ {内容}（无则整节省略）}

—— 由「新人上手手册」整理
```

要点：`□` 复选框样式让接收人可以直接勾选；末尾产品署名是自然的传播位（分享即拉新）。

---

## 四、测试用例

| # | 输入 | 验收点 |
|---|---|---|
| 1 | 一份含寒暄、跑题、重复发言的口语转写稿 | 寒暄跑题全部被丢弃，只剩结论和待办 |
| 2 | 讨论了三个方案最终定一个的记录 | conclusions 只写最终方案 + 一句理由 |
| 3 | 多项待办但部分没指定负责人的记录 | 未指定的标〔负责人待确认〕，没编造人名 |
| 4 | 含"尽快完成"等模糊时间的记录 | deadline 保留原文并标〔时间待确认〕 |
| 5 | 一段完全不是会议记录的文本（如一篇散文） | AI 明确说"这不像会议记录"，不硬提炼 |

录音链路单独测：30 秒清晰人声 mp3 → 转写准确率目测 ≥ 90% → 回显可编辑 → 提炼正常。
