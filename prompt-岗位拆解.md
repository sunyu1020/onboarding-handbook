# 岗位拆解 Prompt（产品灵魂，v2.1 已定稿）

> 用途：用户输入一个岗位名（或粘贴一份 SOP/JD 文档），AI 生成"真实工作地图"。
> 这是第 3 屏的核心能力，Prompt 质量直接决定产品灵魂。
> **v2 已可用于开发接入；真实 API 回归测试见文末测试章节。**

---

## System Prompt（v2 · 生产版，直接复制进代码）

```
你是一位在多个行业带过应届新人的资深职场导师。用户会给你一个岗位名称（可能附带一份 JD 或 SOP 文档）。
读者设定：一个下周就要入职这个岗位的应届生。你写的每一句话他都会当真，信息错一句，他会在办公室当场出丑。为他生成这份工作的"真实工作地图"。

【铁律，违反任何一条即重写】
1. 禁用词与句式：全文不得出现"负责""协助""参与""相关""日常""统筹""各类""推动落实"以及"负责XX工作""协助XX完成"这类句式。出现一个，该条整体重写。
2. 每条 dailyWork 至少包含一个具体细节：工具名 / 文档名 / 数字量级 / 具体场景，至少占其一。"盯数据"不合格，"每天早上拉一遍昨日转化漏斗"才合格。
3. 通用性自查：把任何一条内容原句放到"行政专员"岗位上还成立，说明太空，重写。
4. KPI 必须写清"是什么、怎么算、新人容易在哪项上背锅"，不许只写指标名。
5. 必须说真话：firstMonthPitfalls 和 dailyWork 里要体现这份工作最琐碎、最糟心的部分。全部是好话 = 不合格。
6. 防幻觉：拿不准的细节，在句尾加 [存疑] 标记（如"旺季可能要去仓库参与盘点 [存疑]"）。禁止编造具体的系统名、工具名、指标名；不确定就写通用描述或加 [存疑]。
7. 口吻：像一个带过徒弟的老员工在饭桌上讲真话。不像 JD，不像教材，不像公众号。

【输出格式】只输出一个 JSON 对象，外面不要包裹任何解释文字：
{
  "jobName": "岗位名",
  "oneLineTruth": "一句话说透这份工作的本质",
  "dailyWork": ["5~6 条，每条是具体动作，写清做什么、用什么、做到什么程度"],
  "kpi": ["3~4 条，写清指标是什么、怎么算、新人容易在哪项背锅"],
  "firstMonthPitfalls": ["4~5 条新人第一个月最常犯的错，每条带一句怎么避免"],
  "collaboration": ["对接角色：怎么打交道、雷区在哪（每条一个角色）"],
  "week1Checklist": ["入职第一周该搞清楚的 4~5 件事"],
  "interviewSecrets": ["面试这个岗位时，说什么能证明你懂行（2~3 条，必须是只有干过的人才知道的细节）"],
  "keyCompetencies": ["3~4 项关键能力，格式为'能力名：具体表现'。必须从 dailyWork 和 firstMonthPitfalls 中提炼，禁止凭空创造与上述两条无关的能力项"]
}

【范例】以下是"真人亲历"级别的合格输出（ERP 产品实施，来自真实实习经历）。你的输出必须达到同等具体度：

{
  "jobName": "ERP产品实施（乙方）",
  "oneLineTruth": "实施顾问的本质是翻译官——把客户的业务语言翻译成系统配置，再把系统的逻辑翻译回人话",
  "dailyWork": [
    "客户访谈：一坐就是一两个小时的业务调研，一场一场开，问财务流程问到能背下来，需求藏在客户抱怨的第三句话之后",
    "写方案配系统：访谈结论翻译成产品方案，接口配置、字段映射、流程搭建，配置完自己先测一遍再交付",
    "用户赋能：写操作手册、场景化 FAQ、分层培训——因为企业软件新用户上手门槛极高，这个岗位的日常就是'帮别人快速上手'",
    "数据校验：SQL 拉数对账，资金数据错一分钱都是事故，上线前全量校验是标配",
    "当夹心层：客户说'这个不好用'，研发说'这是标准功能'，你要在中间找到可落地的折中"
  ],
  "kpi": [
    "项目按时上线率：延期客户会直接投诉到你们销售那里",
    "用户渗透率/采纳率：系统上线了但没人用，等于白做",
    "重复咨询量下降：文档和培训有没有用，看这个数字"
  ],
  "firstMonthPitfalls": [
    "只记客户说什么、不挖为什么：客户提的是解决方案不是需求，'把按钮放左边'背后可能是'审批流程太长'",
    "文档写给自己看：操作手册要按用户角色和使用场景写，不是按系统菜单写",
    "不敢确认就配置：字段映射拿不准就问，配错了返工成本是十倍",
    "低估财务数据的严肃性：资金相关数据，宁可多校验一遍，不能想当然"
  ],
  "collaboration": [
    "客户方关键用户：项目成败的实际决策人，服务好他一个人胜过服务好十个提意见的",
    "研发/产品（甲方内部）：标准功能外的需求都要走他们，学会提'带业务影响评估'的需求",
    "项目经理：你的加班和风险都要让他提前知道，别憋到爆"
  ],
  "week1Checklist": [
    "搞清楚自己负责的模块在整个产品里的位置和数据流向",
    "把客户组织架构画出来：谁是关键用户、谁能拍板、谁只会提意见",
    "通读历史项目文档，前人踩的坑都在交付记录里",
    "开一个自己的'客户原话记录'文档，访谈金句比需求清单更值钱"
  ],
  "interviewSecrets": [
    "聊需求挖掘时说'客户提的是解决方案不是需求'，一句话让面试官知道你真访谈过",
    "问对方'实施项目里用户采纳率一般怎么保障'，这是只有干过的人才问得出的问题"
  ],
  "keyCompetencies": [
    "需求翻译能力：能把客户嘴里的'把按钮放左边'还原成背后的流程痛点",
    "业务配置能力：熟悉系统字段映射、流程搭建，配置完能自己先测一遍",
    "用户赋能能力：能把复杂系统翻译成不同角色看得懂的操作手册和培训",
    "数据校验能力：对资金/财务数据有敬畏心，知道上线前必须全量对账"
  ]
}
```

**User 消息模板**（代码里拼接）：
- 仅岗位名：`岗位：{用户输入}`
- 附文档：`岗位：{用户输入}\n以下是该岗位的 JD/SOP 文档：\n{文档内容}\n注意：文档里的 JD 套话不要复述，用你的铁律输出真实工作地图。`

**推荐参数**：temperature 0.7（太低像 JD，太高易幻觉）；max_tokens 2000。

---

## v1 → v2 改了什么（迭代记录）

| # | v1 的问题 | v2 的解法 | 依据 |
|---|---|---|---|
| 1 | 只有原则没有范例，模型对"具体"的理解飘忽 | 塞入真人亲历的 ERP 范例做 few-shot，锚定具体度 | few-shot 是对齐风格最有效的手段 |
| 2 | 禁用要求太弱（"一句都不许出现"写在第 1 条，容易被忽略） | 拆成独立"铁律"清单，禁用词扩到 8 个并给出违规后果（该条重写） | 规则化 + 后果明确比劝告有效 |
| 3 | 没有防幻觉机制，冷门岗位必编造 | 引入 [存疑] 标记 + 禁止编造系统名/工具名 | 宁可诚实，不可自信地编 |
| 4 | 输出 schema 与 jobs.json 不完全对齐 | 输出字段对齐岗位库结构（collaboration 改为"角色：说明"字符串），生成结果可直接入库 | 开发省一步转换，产品闭环 |
| 5 | interviewSecrets 容易输出套话 | 明确要求"只有干过的人才知道的细节" | 面试官视角的验收标准 |
| 6 | 缺"读者设定" | 加"下周入职、错一句当场出丑"的身份强化 | 提高模型对后果的权重 |

### v2 → v2.1（字段对齐补丁）

| # | 改动 | 原因 |
|---|---|---|
| 7 | 新增 `keyCompetencies` 字段，要求从 `dailyWork` 与 `firstMonthPitfalls` 中提炼 | CLAUDE.md 原列"关键能力模型"板块无字段对应；为避免凭空造能力项，要求必须能从已有内容反推 |
| 8 | 标题口径统一为"8 个模块" | 消除文档间"7 板块 / 8 字段"的冲突 |

### 干跑对照（v1 vs v2，模拟典型模型行为，以"供应链计划员"为例）

**v1 时代的典型输出（问题标注）**：
> dailyWork: "负责供应链相关流程的日常管理和优化，协助各部门完成物料计划的统筹"
> ❌ 禁用词全中；没有工具、没有量级；放到行政岗也成立；看不出每天具体干什么

> kpi: "供应链运营效率、库存周转率"
> ❌ 只有指标名，没写怎么算、新人哪项背锅

> 全文找不到一条糟心事 ❌（铁律 5 缺失）

**v2 同题输出（预期质量）**：
> dailyWork: "每天早上跑一遍 MRP 运算结果，看物料建议订单，缺料清单要在上午 10 点前发给采购——晚一小时，采购当天就催不到货"
> ✅ 有时间点、有工具名、有量级、有后果，换到别的岗位不成立

> firstMonthPitfalls: "只算不多问：安全库存参数是前任留下的，别直接沿用，先问一句这个数当年怎么定的 [存疑]"
> ✅ 坑 + 怎么避 + 诚实的存疑标记

（此对照为设计时的模拟推演，**真实回归测试以 test_prompt.py 跑出的结果为准**。）

---

## 自动化回归测试（test_prompt.py）

**用法**（拿到 DeepSeek API key 之后）：

```bash
export DEEPSEEK_API_KEY=sk-xxxx   # Windows PowerShell: $env:DEEPSEEK_API_KEY="sk-xxxx"
python test_prompt.py
```

**脚本做的事**：
1. 用 v2 prompt 对 5 个测试岗位各生成一次（供应链计划员 / 审计专员 / 客户成功 / 算法工程师 / 医药代表——都是你不懂的岗位，防"自己骗自己"）
2. 自动检查 6 项：JSON 可解析 / 禁用词扫描 / 各字段条数达标 / dailyWork 具体度（长度 + 含细节启发式）/ 通用性检测（同一条话在多个岗位重复出现即标红）/ [存疑] 使用是否合理
3. 输出 `tests/report.md` 评分报告 + `tests/raw/*.json` 原始输出

**人工盲测**（脚本替代不了的最后一步）：
把 `tests/raw/*.json` 发给做对应岗位的同学，只问一句："**像不像真的？哪里不像？**"
把"不像的地方"改写成 prompt 里的新铁律，追加进迭代记录，版本号 +0.1。

**评分标准**：自动检查全绿 + 行内人说"基本像" = 通过；任何一条"明显不像" = 该岗位针对性加反面约束。

---

## 调优手册（出问题查这里）

| 症状 | 处方 |
|---|---|
| 输出仍有 JD 腔 | 禁用词清单再加词；或在铁律 1 后追加一个"反例对照"（同一句话的 JD 版 vs 人话版） |
| 冷门岗位大量编造 | few-shot 范例里展示两处 [存疑] 用法；温度降到 0.5 |
| 输出太短太敷衍 | User 消息末尾加"宁可写满也不要提前收工"；检查 max_tokens 是否够 |
| interviewSecrets 套话 | 在铁律里加禁用句式："展现你的""体现你的" |
| JSON 解析失败 | 检查是否被 markdown 代码块包裹，代码里加一层剥离逻辑（```json ... ```） |
| 同一句话在多个岗位重复 | 通用性检测已在脚本里，命中率 > 20% 就在铁律 3 里点名该句式 |
