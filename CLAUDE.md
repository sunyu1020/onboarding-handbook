# CLAUDE.md — 项目说明书（登陆岛）

> 本文档是给 AI 编程助手的项目需求文档。每次对话前请完整阅读本文档，所有开发决策以本文档为准。当需求与本文档冲突时，先提醒我确认，不要自行发挥。

## 一、产品定位

- **产品名**：登陆岛（已定名，slogan「不漂流，来登陆」）
- **一句话定位**：帮助只有实习经历的应届毕业生，跨过"拿到 offer"到"真正会干活"之间的认知断层
- **目标用户**：大四应届生 / 入职 0-12 个月的新人，有 1-3 段实习经历，入职后面临"不知道工作到底要干什么"的恐慌
- **核心洞察**：新人的痛不是"效率不够"，而是"认知空白"——日常做什么、KPI 怎么算、领导那句话什么意思、周报怎么写，这些隐性知识书本不教、同事懒得讲、不好意思问
- **AI 角色**：AI 是核心引擎而非附加功能——把隐性职场知识显性化、按岗位个性化交付。任何一个模块如果去掉 AI 也能完整使用，说明该模块的 AI 设计失败了，请提醒我重新设计
- **长期方向**（仅作背景理解，不体现在页面）：C 端免费获客 → 沉淀新人上手场景 → B 端企业新人培训（Onboarding）市场

## 二、体验架构总览

单页沉浸式滚动 + 悬浮导航锚点。共 8 屏，滚动方向自上而下依次展开。

导航：已升级为 React 浮窗导航 `FloatingNav`（`src/components/ui/floating-nav.tsx`，fixed + z-50 跨屏可见、可拖拽/锁定），锚点 6 个（工作台 / 岗位拆解 / 台账 / 会议纪要 / 汇报 / 开挂室），配置在 `src/components/ui/nav-items.ts`。

## 三、逐屏需求

### 第 1 屏：开场（已 React 化）
- 已重做为「登陆岛」Hero：`src/hero-react.tsx` 挂载 `PrismaHero`（`src/components/ui/prisma-hero.tsx`）到 `#prisma-hero-root`，h-screen 撑满
- 大标题「登陆岛」+ slogan「不漂流，来登陆」/「把陌生的海域走成自己的岛」
- 字体：Press Start 2P + Noto Sans SC（Google Fonts）

### 第 2 屏：今日工作台
- 按访问时段显示问候语（早上好/下午好/晚上好），附一句与新人场景相关的问候文案
- 顶部输入框「粘贴你的工作排期/待办」（支持粘贴纯文本；飞书链接解析做不了就先做纯文本粘贴，不要伪造功能）
- 四大统计卡片：进行中事项（数量+本周新增）/ 待办（未完成+今日到期）/ 延期风险项（数量，可跳转台账）/ 本周完成度（环形进度图）
- 本月日历视图：标出本周关键节点日期，当日有呼吸动画边框
- 最近动态时间线：倒序最新 6 条（新增待办/生成纪要/导出周报等），含图标、名称、时间

### 第 3 屏：岗位拆解 ★ 全站核心功能（v2 已上线）
- **双角色卡片入口**（先选角色，再执行动作）：两张卡片——「岗位名拆解」（输入岗位名）/「JD 拆解 Agent」（粘贴 JD 原文 / 上传 JD 截图）
- 岗位名拆解：调用 DeepSeek API，生成 8 个模块的可视化卡片（与 `prompt-岗位拆解.md` v2.1 输出字段严格对齐）：
  1. oneLineTruth · 一句话说透岗位本质
  2. dailyWork · 日常工作清单（一天/一周实际在干什么，要具体到动作）
  3. kpi · 常见 KPI 与考核方向（数字怎么算的、新人容易在哪项背锅）
  4. firstMonthPitfalls · 容易踩的坑（新人第一个月高频错误 + 怎么避免）
  5. collaboration · 与哪些部门/角色打交道、怎么打交道
  6. week1Checklist · 入职第一周该搞清楚的 4~5 件事
  7. interviewSecrets · 面试这个岗位时，说什么能证明你懂行
  8. keyCompetencies · 关键能力模型（由 dailyWork 与 firstMonthPitfalls 提炼出的 3~4 项核心能力）
- JD 拆解 Agent（详见 `prompt-JD拆解Agent.md`，三个 Prompt 已逐字进 `src/prompt-jd-*.txt`）：粘贴 JD 文本 / 上传 JD 截图（GLM-4V-Flash 提字，回显可编辑）→ 首次拆解（8 字段 + jdDecoded「JD 没写的大实话」板块）→ 追问对话模式（流式打字机，3 个快捷追问按钮）
- 生成结果支持收藏到本地岗位库（localStorage），可反复查看；预置 29 岗正方形网格 + 查看更多/收起
- **质量要求**：输出必须像"真实工作地图"，禁止 JD 式的空话（如"负责相关工作的推进与落实"）。Prompt 已定稿（`prompt-岗位拆解.md` v2.1、`prompt-JD拆解Agent.md`），请严格使用

### 第 4 屏：项目台账
- 与第 2 屏排期数据联动，自动汇总
- 字段：项目名称 / 进度阶段（大纲→脚本→拍摄→初稿→发布，做成进度条）/ 关键节点日期 / 负责人 / 风险项
- 支持粘贴工作清单文本，自动整理：统一日期与名称格式、去重
- 已延期或有风险的行标红
- 按阶段分列卡片视图 + 按项目/负责人筛选
- 数据 localStorage 持久化

### 第 5 屏：会议纪要
- 上传 Word/PDF/文本格式的纪要文档（docx 走 mammoth、pdf 走 pdfjs-dist，动态 import），或上传录音（mp3/wav/m4a，≤50MB）
- **录音链路（已定案并实测打通）**：本地 AudioContext 解码 → 重采样 16kHz 单声道 PCM16 → DashScope **paraformer-realtime-v2** 实时 WS 流式识别（`?api_key=` query 鉴权，协议要点：task_group/task/model 在 payload、事件在 header.event、task-started 后才推流）→ 转写结果回显可编辑 → 统一走提炼链路。详见 `prompt-会议纪要.md`
- 调用 DeepSeek API 提炼结构化清单：只保留最终结论 / 待办事项 / 每项负责人 / 截止时间，按优先级排序，忽略跑题寒暄
- 输出可视化清单 + 「一键复制成可发群的纯文本」（严格按 `prompt-会议纪要.md` 第三节模板）+「待办同步到工作台」（去重后写入 odb_todos）
- 历史纪要存 localStorage（最近 20 条，可展开/删除），供第 6 屏周报引用

### 第 6 屏：周报与向上汇报（两个子标签）
- **分工原则**：`buildWeeklySnapshot()` 前端计算本周数据快照（完成/延期/风险/下周计划/纪要/项目），DeepSeek 只负责把快照组织成文（铁律：不许编造快照里没有的数据）
- 「生成本周周报」：三段式（本周完成 / 延期与风险 / 下周计划），重点用数据说话；顶部统计条含本周完成度环形图；本周已有周报时按钮变「重新生成」需确认覆盖
- 「生成向上汇报」：基于周报当前内容改写为 80~150 字适合发领导的版本；回填到同一周的 `odb_reports` 记录
- 两个按钮都调用 DeepSeek API（纯文本输出，不走 json_object），生成结果可编辑、可复制；历史按周倒序保留 8 条

### 第 7 屏：新人开挂室（两个子标签）
- 「充电区·新人书单」（v2 已升级）：20 本精选书，卡片显示**真实封面图**（coverUrl，加载失败自动降级为藏书票文字层）；悬停放大（1.04）/选中放大（1.06）+ 主题描边；悬停弹出**悬浮详情浮层**（不占布局空间：书名/作者/标签/内容概括/豆瓣真实短评/读书笔记），点击卡片打开**页内全屏阅读面板**（3 页翻页：这本书讲什么 / 读者怎么说 / 开始阅读，Esc/左右键/按钮翻页）；左上角圆点标记想读/在读/已读（点击循环切换，写 `odb_book_status`）
- **版权红线**：不托管、不跳转任何盗版全文；正文阅读唯一入口为「微信读书」正版外链，另有「豆瓣详情」条目页外链
- 书单推荐逻辑：按 `books.json` 的 priority（P0 必读 6 本 / P1 进阶 9 本 / P2 拓展 5 本）→ readingStage（入职前/入职 1 个月/入职 3 个月）→ 标题排序；支持按岗位与阅读阶段筛选
- 「破局区·新人困境」：困境输入框（本地关键词匹配，不调 AI）+ 高频困境标签（与 `data/dilemmas.json` 16 条一一对应）。选择后给出三套风格不同的解法（**强硬 / 迂回 / 共赢**，与 JSON 的 style 字段严格一致，前端按此渲染三栏），三栏卡片并排，每套写清：具体怎么说（say 话术）/ 怎么做（do 步骤）/ 适用场景（scene），末尾各一句分寸提醒（caution）
- 困境解法库做成 JSON 配置，支持收藏（`odb_dilemma_fav`）+「只看已收藏」筛选

### 第 8 屏：关于 & 初衷
- 极简布局：创作初衷（我是一个大四学生，经历了"拿到 offer 却不知道怎么干活"的慌，不想让和我一样的人再经历）、使用小贴士
- 一句收尾文案："你已经比昨天的自己更接近会干活了"
- 标题两侧线条点缀，底部细线分隔
- 底部「数据管理」区块：导出我的数据（10 个 localStorage key 打包 JSON 下载）/ 导入数据（校验 app 标识 + 摘要确认 + 导入前自动备份 + reload）/ 清空全部数据（两次 confirm，第二次明确不可恢复）

## 四、技术约束

- **纯前端单页应用**。技术栈已切换为：**Vite + React 19 + TypeScript + Tailwind CSS 4**（配 framer-motion、lucide-react）。原 8 屏为原生 JS 实现，正逐步迁移到 React（当前已迁移：第 1 屏 Hero + 全站浮窗导航；`src/hero-react.tsx`、`src/components/`、`src/lib/`）
- 部署平台：GitHub Pages，**GitHub Actions 自动部署**（`.github/workflows/deploy.yml`：push main → npm ci → build → deploy）。已上线：https://sunyu1020.github.io/onboarding-handbook/
- 数据全部存 localStorage，按类目独立 key，共 10 个：`odb_todos`（待办）/ `odb_activities`（动态）/ `odb_projects`（台账）/ `odb_meetings`（纪要）/ `odb_reports`（周报）/ `odb_book_status`（书单状态）/ `odb_dilemma_fav`（困境收藏）/ `odb_book_notes`（读书笔记）/ `job_library`（AI 岗位收藏）/ `preset_favorites`（预置收藏）。第 8 屏支持一键导出/导入/清空
- 内容配置化：岗位库、书单、困境库全部放独立 JSON 文件（`data/jobs.json`、`data/books.json`、`data/dilemmas.json`），改内容不需要改代码；JSON 数据文件为只读配置，用户状态一律写 localStorage
- AI 调用，三个服务均已定案，不再评估替代方案：
  1. **DeepSeek**（deepseek-chat）：主力模型——岗位拆解、JD 拆解与追问、纪要提炼、周报/向上汇报生成
  2. **智谱 GLM-4V-Flash**（免费）：JD 截图提字，Prompt 见 `prompt-JD拆解Agent.md`
  3. **阿里百炼 DashScope ASR**（paraformer-realtime-v2 实时 WS 流式识别）：会议录音转写，方案见 `prompt-会议纪要.md`
- 所有 API Key 通过 `.env` 文件读取（模板见 `.env.example`），`.env` 必须写进 `.gitignore`（已配置）。前端直连即可（作品集 demo 场景，key 暴露风险已知；**站点已公网部署，key 会出现在线上 bundle 中，建议上线后轮换 key**）
- 深浅色模式切换，偏好持久化，默认跟随系统（配色预览为临时控件，URL 带 `?theme=1` 唤出，定稿后整体删除）
- 完美适配移动端（媒体查询 + 触摸友好的点击区域）

## 五、视觉与文案基调

- 视觉（全站统一：奶油毛玻璃 Cream Glassmorphism）
  - 基调：温暖、干净、清晰、有条理；大量留白，卡片化布局。
  - 材质：以「奶油毛玻璃」为唯一核心视觉语言——半透明奶油面板（≈ rgba(225,224,204,0.10)）+ backdrop-filter: blur(16px) saturate(140%) + 1px 奶油描边（≈ rgba(225,224,204,0.28)）+ 柔和投影（0 8px 32px rgba(0,0,0,0.45)），统一落在深底（纯黑 #000 / 深岩灰）之上。8 屏视觉语言一致，不再混用像素霓虹。
  - 主色：单一温暖奶油强调色 #E1E0CC（文字 / 描边 / 主按钮实底深字）；次级信息用低饱和奶油灰。删除原 blue / orange / green 三套霓虹临时主题（含 .theme-preview 切换器，定稿后整体移除）。
  - 交互：hover 微上浮（-2px）+ 阴影加深，展开/收起带过渡动画（0.2–0.3s ease）。
  - 禁止：花哨渐变背景、粒子特效、硬阴影霓虹描边、与毛玻璃冲突的高饱和实色块。
  - 深浅色：跟随系统、偏好持久化；毛玻璃材质在深浅两模式下统一成立（深底奶油玻璃为主）。
- 文案：短句为主，一句能说清不写两句。调性"理性克制、干练温暖有力量"，不鸡汤、不煽情、不用感叹号轰炸
- 所有用户可见文案先写进文案表给我过目，尤其是问候语和第 8 屏初衷

## 六、开发规矩（必须遵守）

1. **一次只做一个任务**：我每次只安排一个屏或一个功能，做完停下来等我验收，不要连续开发多个屏
2. **动手前先说方案**：接到任务先简述实现思路（用什么组件、数据结构长什么样），我确认后再写代码
3. **每个功能完成后**：告诉我如何在浏览器里验证（具体操作步骤 + 预期看到什么），并建议一条 git commit 信息
4. **出错处理**：我贴报错原文时，先解释错误原因（用人话），再修复，不要直接甩一坨新代码
5. **不伪造功能**：没接通 AI 的模块就显示明确的引导状态，不要用假数据假装能生成
6. **不改需求之外的东西**：发现其他问题时先提醒我，经确认再改

## 七、当前阶段备忘

- [x] 阶段 0：环境搭建与 Claude Code 连通 —— 已完成
- [x] 阶段 1：项目初始化 + 本文档完善 —— 已完成（git 仓库 + Vite 项目已建）
- [x] 阶段 2：内容资产 —— 已完成（岗位库 29 个含 keyCompetencies / 困境库 16 条三套解法 / 书单 20 本含 priority+coverUrl+summary+reviews / 岗位拆解 Prompt v2.1 / JD 拆解 Agent Prompt / 会议纪要 Prompt）
- [x] 阶段 3：逐屏开发 —— 已完成（8 屏全部开发完毕，含第 3 屏 v2 JD 拆解 Agent 角色卡片双入口、第 6 屏周报数据快照、第 7 屏书单 v2 升级、第 8 屏数据管理）
- [x] 阶段 4：AI 服务接入 —— 已完成（DeepSeek 岗位拆解/JD 拆解与追问/纪要/周报 ✅、DashScope paraformer-realtime-v2 实时录音转写实测打通 ✅、智谱 GLM-4V-Flash 截图提字 ✅）
- [x] P0 收尾轮 —— 已完成（配色预览隐藏 ?theme=1 唤出 / favicon+OG 元信息 / 数据导出导入清空 / 空态统一组件 / 移动端功能性走查）
- [x] 正式发布 —— 已上线 GitHub Pages（GitHub Actions 自动部署）：https://sunyu1020.github.io/onboarding-handbook/
- [ ] 阶段 5：样式阶段 —— 进行中（产品已更名「登陆岛」、技术栈切换 React+Tailwind+TS；第 1 屏 Hero 与全站浮窗导航已 React 化，其余 7 屏样式待做；配色预览定稿后删除；og:image 分享图待出）

> 遗留项（人工任务，AI 不可代劳）：
> 1. 岗位库 9 个 AI 初稿岗位待行内人校验（优先：银行柜员、教师、审计），hr-specialist 素材来源待确认
> 2. `test_prompt.py` 待填入真实 DeepSeek key 跑回归测试
> 3. 找 2-3 位行内人盲测岗位拆解 Prompt 输出质量
> 4. 站点已公网部署，API key 会出现在线上 bundle 中——建议尽快去三个平台轮换 key
> 5. CLAUDE.md 视觉与文案基调章节（五）已定稿为「奶油毛玻璃」规范（深底 + 单一奶油强调色 #E1E0CC，删除三套霓虹临时主题）——本遗留项已闭环。

> 每完成一个阶段，请提醒我更新本节打勾，并把新决策补充进对应章节。
