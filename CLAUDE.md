# CLAUDE.md — 项目说明书（新人上手手册）

> 本文档是给 AI 编程助手的项目需求文档。每次对话前请完整阅读本文档，所有开发决策以本文档为准。当需求与本文档冲突时，先提醒我确认，不要自行发挥。

## 一、产品定位

- **产品名**：新人上手手册（暂定，可在开发中给我起名建议）
- **一句话定位**：帮助只有实习经历的应届毕业生，跨过"拿到 offer"到"真正会干活"之间的认知断层
- **目标用户**：大四应届生 / 入职 0-12 个月的新人，有 1-3 段实习经历，入职后面临"不知道工作到底要干什么"的恐慌
- **核心洞察**：新人的痛不是"效率不够"，而是"认知空白"——日常做什么、KPI 怎么算、领导那句话什么意思、周报怎么写，这些隐性知识书本不教、同事懒得讲、不好意思问
- **AI 角色**：AI 是核心引擎而非附加功能——把隐性职场知识显性化、按岗位个性化交付。任何一个模块如果去掉 AI 也能完整使用，说明该模块的 AI 设计失败了，请提醒我重新设计
- **长期方向**（仅作背景理解，不体现在页面）：C 端免费获客 → 沉淀新人上手场景 → B 端企业新人培训（Onboarding）市场

## 二、体验架构总览

单页沉浸式滚动 + 顶部悬浮导航锚点。共 8 屏，滚动方向自上而下依次展开。

顶部导航：向下滚动隐藏、向上滚动渐显，仅显示核心锚点（工作台 / 岗位拆解 / 台账 / 汇报 / 成长）。

## 三、逐屏需求

### 第 1 屏：开场
- 居中大标题「新人上手手册」+ slogan（候选："从拿到 offer，到真正会干活" / "入职前 90 天，有人带你走"，最终文案待我确认）
- 主 CTA 按钮「打开我的工作台」，点击平滑滚动到第 2 屏
- 微动效向下箭头，提示下滑
- 背景干净克制，无花哨粒子特效

### 第 2 屏：今日工作台
- 按访问时段显示问候语（早上好/下午好/晚上好），附一句与新人场景相关的问候文案
- 顶部输入框「粘贴你的工作排期/待办」（支持粘贴纯文本；飞书链接解析做不了就先做纯文本粘贴，不要伪造功能）
- 四大统计卡片：进行中事项（数量+本周新增）/ 待办（未完成+今日到期）/ 延期风险项（数量，可跳转台账）/ 本周完成度（环形进度图）
- 本月日历视图：标出本周关键节点日期，当日有呼吸动画边框
- 最近动态时间线：倒序最新 6 条（新增待办/生成纪要/导出周报等），含图标、名称、时间

### 第 3 屏：岗位拆解 ★ 全站核心功能，投入最高优先级
- 顶部输入框「输入你的岗位」（如：新媒体运营/产品经理/销售/行政）+ 支持粘贴岗位 SOP 文档，自动拆解后加入岗位列表
- 输入后调用 DeepSeek API，生成 8 个模块的可视化卡片（与 `prompt-岗位拆解.md` v2 输出字段严格对齐）：
  1. oneLineTruth · 一句话说透岗位本质
  2. dailyWork · 日常工作清单（一天/一周实际在干什么，要具体到动作）
  3. kpi · 常见 KPI 与考核方向（数字怎么算的、新人容易在哪项背锅）
  4. firstMonthPitfalls · 容易踩的坑（新人第一个月高频错误 + 怎么避免）
  5. collaboration · 与哪些部门/角色打交道、怎么打交道
  6. week1Checklist · 入职第一周该搞清楚的 4~5 件事
  7. interviewSecrets · 面试这个岗位时，说什么能证明你懂行
  8. keyCompetencies · 关键能力模型（由 dailyWork 与 firstMonthPitfalls 提炼出的 3~4 项核心能力，前端聚合展示）
- JD 拆解 Agent 对话形态（详见 `prompt-JD拆解Agent.md`）：支持双输入——粘贴 JD 文本 / 上传 JD 截图（截图先经 GLM-4V-Flash 提字，结果回显可编辑后再提交），首次拆解后进入追问对话模式
- 生成结果支持收藏到本地岗位库（localStorage），可反复查看
- **质量要求**：输出必须像"真实工作地图"，禁止 JD 式的空话（如"负责相关工作的推进与落实"）。这是本产品灵魂，Prompt 需要反复调试，我会在阶段 2 提供打磨好的 Prompt 和 few-shot 范例，请严格使用

### 第 4 屏：项目台账
- 与第 2 屏排期数据联动，自动汇总
- 字段：项目名称 / 进度阶段（大纲→脚本→拍摄→初稿→发布，做成进度条）/ 关键节点日期 / 负责人 / 风险项
- 支持粘贴工作清单文本，自动整理：统一日期与名称格式、去重
- 已延期或有风险的行标红
- 按阶段分列卡片视图 + 按项目/负责人筛选
- 数据 localStorage 持久化

### 第 5 屏：会议纪要
- 上传 Word/PDF/文本格式的纪要文档，或上传录音（mp3/wav/m4a，≤50MB）
- **录音链路（已定案，不再讨论替代方案）**：录音先调阿里百炼 DashScope ASR（sensevoice-v1 / paraformer-v2）转写成文字 → 转写结果回显到可编辑文本框（允许用户手动修正，ASR 翻车时用户能自救）→ 再统一走提炼链路。详见 `prompt-会议纪要.md`
- 调用 DeepSeek API 提炼结构化清单：只保留最终结论 / 待办事项 / 每项负责人 / 截止时间，按优先级排序，忽略跑题寒暄
- 输出可视化清单 + 「一键复制成可发群的纯文本」按钮
- 结果存入 localStorage，供第 6 屏周报引用

### 第 6 屏：周报与向上汇报（两个子标签）
- 「生成本周周报」：自动汇总台账进展 + 会议纪要待办，输出三段式：本周完成项 / 延期与风险 / 下周计划，重点用数据说话；顶部自动计算本周完成度百分比
- 「生成向上汇报」：基于周报内容生成适合发给领导的版本，突出工作重点、成果和下周规划
- 两个按钮都调用 DeepSeek API，生成结果可编辑、可复制

### 第 7 屏：新人开挂室（两个子标签）
- 「充电区·新人书单」：藏书票风格，3-5 列自适应错落布局，20 本精选书（职场思维/沟通表达/向上管理/认知科学），书名+作者做封面，左上角圆点标记想读/在读/已读，点击弹出抽屉式详情（推荐理由/标签/在线搜索按钮）
- 书单推荐逻辑：按 `books.json` 的 priority（P0 必读 6 本 / P1 进阶 9 本 / P2 拓展 5 本）+ forRoles（适配岗位）排序，回答"先看哪本"；支持按岗位与阅读阶段（入职前/入职 1 个月/入职 3 个月）筛选
- 「破局区·新人困境」：困境输入框 + 高频困境标签（与 `data/dilemmas.json` 16 条一一对应：不敢问问题/汇报紧张/插不进话/被放养/被安排杂活/打杂没含金量/活与 JD 不符/学生思维/领导话听不懂/指令有问题不敢提/被甩锅/被抢功/跨部门拖/被边缘化/加班内耗/无反馈）。选择后给出三套风格不同的解法（**强硬 / 迂回 / 共赢**，与 JSON 的 style 字段严格一致，前端按此渲染三栏），三栏卡片并排，每套写清：具体怎么说（say 话术）/ 怎么做（do 步骤）/ 适用场景（scene），末尾各一句分寸提醒（caution）
- 困境解法库做成 JSON 配置，支持收藏

### 第 8 屏：关于 & 初衷
- 极简布局：创作初衷（我是一个大四学生，经历了"拿到 offer 却不知道怎么干活"的慌，不想让和我一样的人再经历）、使用小贴士
- 一句收尾文案（候选："你已经比昨天的自己更接近会干活了"）
- 标题两侧线条点缀，底部细线分隔

## 四、技术约束

- **纯前端单页应用**，技术栈已定：Vite + 原生 JavaScript（不再更换）
- 部署平台已定：GitHub Pages（`npm run build` 产出 dist/，推送到 gh-pages 分支发布）
- 数据全部存 localStorage，按类目独立 key（待办/台账/纪要/周报/岗位库/书单状态/困境收藏），结构清晰可导出
- 内容配置化：岗位库、书单、困境库、问候语文案全部放独立 JSON 文件（如 `data/books.json`、`data/dilemmas.json`），改内容不需要改代码
- AI 调用，三个服务均已定案，不再评估替代方案：
  1. **DeepSeek**（deepseek-chat）：主力模型——岗位拆解、JD 拆解与追问、纪要提炼、周报/向上汇报生成
  2. **智谱 GLM-4V-Flash**（免费）：JD 截图提字，Prompt 见 `prompt-JD拆解Agent.md`
  3. **阿里百炼 DashScope ASR**（sensevoice-v1 / paraformer-v2）：会议录音转写，方案见 `prompt-会议纪要.md`
- 所有 API Key 通过 `.env` 文件读取（模板见 `.env.example`），`.env` 必须写进 `.gitignore`（已配置）。前端直连即可（作品集 demo 场景，我知晓 key 暴露风险）
- 深浅色模式切换，偏好持久化，默认跟随系统
- 完美适配移动端（媒体查询 + 触摸友好的点击区域）
- 依赖尽量少，不引入大型 UI 框架也能做就不引入

## 五、视觉与文案基调

- 视觉：干净、清晰、有条理。大量留白，卡片化布局，克制的主色调（建议给我 2-3 套配色方案选择）。hover 微上浮+阴影加深，展开/收起带过渡动画，禁止花哨渐变和粒子特效
- 文案：短句为主，一句能说清不写两句。调性"理性克制、干练温暖有力量"，不鸡汤、不煽情、不用感叹号轰炸
- 所有用户可见文案先写进文案表给我过目，尤其是问候语和第 8 屏初衷

## 六、开发规矩（必须遵守）

1. **一次只做一个任务**：我每次只安排一个屏或一个功能，做完停下来等我验收，不要连续开发多个屏
2. **动手前先说方案**：接到任务先简述实现思路（用什么组件、数据结构长什么样），我确认后再写代码
3. **每个功能完成后**：告诉我如何在浏览器里验证（具体操作步骤 + 预期看到什么），并建议一条 git commit 信息
4. **出错处理**：我贴报错原文时，先解释错误原因（用人话），再修复，不要直接甩一坨新代码
5. **不伪造功能**：没接通 AI 的模块就显示明确的引导状态，不要用假数据假装能生成
6. **不改需求之外的东西**：发现其他问题时先提醒我，经确认再改

## 七、当前阶段备忘

- [x] 阶段 0：环境搭建与 Claude Code 连通 —— 已完成
- [x] 阶段 1：项目初始化 + 本文档完善 —— 已完成（git 仓库 + Vite 项目已建）
- [x] 阶段 2：内容资产 —— 已完成（岗位库 29 个含 keyCompetencies / 困境库 16 条三套解法 / 书单 20 本含 priority / 岗位拆解 Prompt v2.1 / JD 拆解 Agent Prompt / 会议纪要 Prompt）
- [x] 阶段 3：逐屏开发 —— 已完成（8 屏全部开发完毕，提交 6bb722d）
- [x] 阶段 4：AI 服务接入 —— 已完成（DeepSeek 岗位拆解/纪要/周报 ✅、DashScope 录音转写 ✅、智谱 GLM-4V-Flash 截图提字 ✅，提交 a94a5c2）
- [ ] 阶段 5：动效打磨 + 移动端适配 + 作品集叙事页 + 正式发布（GitHub Pages）

> 遗留项（人工任务，AI 不可代劳）：
> 1. 岗位库 9 个 AI 初稿岗位待行内人校验（优先：银行柜员、教师、审计），hr-specialist 素材来源待确认
> 2. `test_prompt.py` 待填入真实 DeepSeek key 跑回归测试
> 3. 找 2-3 位行内人盲测岗位拆解 Prompt 输出质量

> 每完成一个阶段，请提醒我更新本节打勾，并把新决策补充进对应章节。
